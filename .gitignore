# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Dependencies (this project intentionally has none, but just in case)
node_modules/

# Local backups exported during testing (never commit user data)
*-backup-*.json
